/**/mask_to_3d:
  ros__parameters:
    base_frame_name: "base_footprint"
    mask_topic_name: "sam3_ros/object_masks"
    cloud_topic_name: "head_camera/depth/color/points"
    # depth_image_topic_name: "head_camera/depth/image_rect_raw" # TODO: Add depth image option
    info_topic_name: "head_camera/depth/camera_info"

    # Options (TODO)
    # positioning_detection_mode: "point_cloud" # Options: "point_cloud", "fast_point", "depth_image"
    # enable_id: false

    # Clipping box parameters (respect to camera frame)
    x_min: 0.0
    x_max: 2.0
    y_min: -2.0
    y_max: 2.0
    z_min: -2.0
    z_max: 2.0
  
    # Point Cloud Processing
    cluster_tolerance: 0.005
    min_cluster_size: 100
    max_cluster_size: 20000
    noise_point_cloud_range: 0.01
    voxel_leaf_size: 0.005
